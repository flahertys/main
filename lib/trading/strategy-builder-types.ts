/**
 * TypeScript interfaces for the Visual Strategy Builder.
 * Re-exports from the shared trading types for convenience.
 */

export type {
  BlockCategory,
  BlockParam,
  BlockParamType,
  BlockType,
  StrategyBlock,
  StrategyDefinition,
  StrategyValidationResult,
} from "@/types/trading";
