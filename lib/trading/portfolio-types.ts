/**
 * TypeScript interfaces for the Multi-Exchange Portfolio Dashboard.
 * Re-exports from the shared trading types for convenience.
 */

export type {
  ExchangeConnection,
  PortfolioAsset,
  PortfolioSnapshot,
  RebalanceSuggestion,
  SupportedExchange,
} from "@/types/trading";
