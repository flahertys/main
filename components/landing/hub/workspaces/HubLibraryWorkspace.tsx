import React from "react";

type HubLibraryWorkspaceProps = {
  children: React.ReactNode;
};

export function HubLibraryWorkspace({ children }: HubLibraryWorkspaceProps) {
  return <>{children}</>;
}
